{
	"name": "machines.fail",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"start": "astro dev",
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro build && astro preview",
		"deploy": "astro build && gh-pages -d dist -f",
		"format": "prettier --write .",
		"clean": "rm -rf dist"
	},
	"devDependencies": {
		"@astrojs/tailwind": "5.0.0",
		"@tailwindcss/typography": "0.5.10",
		"astro": "3.1.2",
		"gh-pages": "6.0.0",
		"prettier": "3.0.3",
		"prettier-plugin-astro": "0.12.0"
	}
}
